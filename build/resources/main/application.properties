spring.application.name=Task_Manager
jwt.secret=${JWT_SECRET_KEY}
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/taskmanagementsystem


# Database credentials
spring.datasource.username=root
spring.datasource.password=${SQL_PWD}

# Hibernate Dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Connection pool settings (optional)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.initialization-mode=always
spring.security.user.name=reshma
spring.security.user.password=123456

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chennupatireshma@gmail.com
spring.mail.password=${EMAIL_PWD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


